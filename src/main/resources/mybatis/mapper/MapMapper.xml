<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.enjoytrip.map.model.dao.MapDao">

	<select id="getGugun" parameterType="int" resultMap="gugun">
		select
		sidoCode, gugunCode, gugunName
		from gugun
		where sidoCode = #{sidoCode}
	</select>

	<select id="getMarker" parameterType="map" resultMap="marker">
		select
		contentTypeId, title, addr1, addr2, firstImage, readCount, latitude,
		longitude, sidoCode, gugunCode
		from attraction_info
		where contentTypeId
		= #{contentTypeId} and sidoCode = #{sidoCode} and gugunCode =
		#{gugunCode}
	</select>
	
	<select id="getSido" resultType="sidoGugunCodeDto">
		select left(sidoCode,2)
		sidoCode, sidoName
		from sidocode
		order by sidoCode
	</select>

	<select id="getGugunInSido" parameterType="string"
		resultType="sidoGugunCodeDto">
		select left(gugunCode,5) gugunCode, gugunName
		from guguncode
		where left(gugunCode,2) = #{sido}
		order by gugunCode
	</select>
</mapper>
